node_modules
dist/
*.log
.DS_Store

# Playwright (commit only the baseline __screenshots__)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# npm pack output
*.tgz

# consumer gate scratch: staged pack source, candidate tarball, throwaway
# consumer clone (scripts/consumer-gate.mjs — see docs/release-gate.md)
/.relgate/

# exports-import guard scratch: a real packed-and-extracted tarball, cleaned
# up by the test itself — see test/exports-import.test.mjs
/.exports-import-check/

# local throwaway preview pages (served ad-hoc, never committed)
/_preview.html
